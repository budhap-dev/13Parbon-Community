# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test output
coverage

# Local environment
.env.local
.env*.local

# Draft Supabase policies, not ready to ship. The helper functions reference tables before
# they exist, current_setting(...)::jsonb can throw, and the auth.users triggers could block
# sign-in. Kept locally until they are fixed and verified against a real project.
supabase/portal.sql
supabase/verify.sql
