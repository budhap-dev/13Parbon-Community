# Copy to .env.local and fill in from Supabase → Project Settings → API.
# Both must be set for the contact form to submit; otherwise the site
# says so and offers another way through. The anon key is safe to expose.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
